# ai-data-platform environment — copy to .env (never commit .env)

# AI provider key (default provider: MiniMax; see adp.yaml model_provider)
MINIMAX_API_KEY=


# Alternative providers (only the one referenced by api_key_env is used)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=

# Database credentials referenced from adp.yaml via ${VAR}
# PGPASSWORD=
# MYSQL_PASSWORD=
